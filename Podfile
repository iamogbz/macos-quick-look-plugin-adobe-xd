# Uncomment the next line to define a global platform for your project
platform :macos, '10.15'

# Main app target
target 'QuickLookXD' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuickLookXD
  pod 'SSZipArchive', '~> 2.5.0'

end

# Quick Look extension targets - add these after you create the extension targets in Xcode.
# Running `pod install` will link SSZipArchive into the extension targets.

target 'QuickLookXD-Thumbnail' do
  use_frameworks!
  pod 'SSZipArchive', '~> 2.5.0'
end


target 'QuickLookXD-Preview' do
  use_frameworks!
  pod 'SSZipArchive', '~> 2.5.0'
end
